# Vercel installs runtime dependencies from this file at the repository root.
# Mirrors backend/requirements.txt exactly (the runtime deps). The backend itself
# is unchanged and imported via index.py. boto3 stays optional (live AWS path only;
# the demo runs without it).
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
httpx>=0.26,<0.28
mangum>=0.17
segno>=1.6
